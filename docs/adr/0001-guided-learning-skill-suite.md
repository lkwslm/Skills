# 拆分“带我学”和“接着学”

学习流程采用两个可独立调用的 skill：`learn-with-me` 负责校准、引导式推导、课程、自测与回顾，`continue-learning` 负责依据工作区学习档案校验并恢复上下文。运行状态、概念知识和课程内容分别拥有明确事实源，课程以自包含 HTML 保存；这一拆分避免主 skill 膨胀和依赖聊天历史恢复，同时接受两个 skill 必须共享并维护同一档案契约的耦合。
